// Generated by Nitrogen Modules. Do not edit.
// nitro_generator: 0.7.6
// Generated from: nitro_fold_duo.native.dart
// spec-sha256: 0240db7d8a98501cbeea458c605b5e38e7c883ec2ad860e3330f97f958ee4cda
package nitro.nitro_fold_duo_module

import android.app.Activity
import android.content.Context
import androidx.annotation.Keep
import kotlinx.coroutines.flow.Flow
import kotlinx.coroutines.launch
import kotlinx.coroutines.CoroutineScope
import kotlinx.coroutines.CoroutineStart
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.sync.withLock

// --- Enums ---
@Keep
enum class DuoHingeStatus(val nativeValue: Long) {
  UNKNOWN(0),
  CLOSED(1),
  PARTIALLYOPEN(2),
  FULLYOPEN(3);

  companion object {
    fun fromNative(v: Long): DuoHingeStatus = values().first { it.nativeValue == v }
  }
}

@Keep
enum class DuoRegionKind(val nativeValue: Long) {
  OCCLUSION(0),
  DIVISION(1);

  companion object {
    fun fromNative(v: Long): DuoRegionKind = values().first { it.nativeValue == v }
  }
}

@Keep
enum class DuoVerticalBarEdge(val nativeValue: Long) {
  UNSPECIFIED(0),
  LEADING(1),
  TRAILING(2);

  companion object {
    fun fromNative(v: Long): DuoVerticalBarEdge = values().first { it.nativeValue == v }
  }
}

// --- @HybridRecord Kotlin data classes (generated by Nitrogen) ---

@androidx.annotation.Keep
data class DuoInsets(val left: Double, val top: Double, val right: Double, val bottom: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): DuoInsets {
            val left = buf.double
            val top = buf.double
            val right = buf.double
            val bottom = buf.double
            return DuoInsets(left, top, right, bottom)
        }
        @JvmStatic fun decode(bytes: ByteArray): DuoInsets {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): DuoInsets = DuoInsets(
                left = (map["left"] as Number).toDouble(),
                top = (map["top"] as Number).toDouble(),
                right = (map["right"] as Number).toDouble(),
                bottom = (map["bottom"] as Number).toDouble()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(32) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeDouble(left)
        writeDouble(top)
        writeDouble(right)
        writeDouble(bottom)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, DuoInsets> support ---
    fun toJson(): Map<String, Any?> = mapOf("left" to left, "top" to top, "right" to right, "bottom" to bottom)

}

@androidx.annotation.Keep
data class DuoReservedRegion(val kind: DuoRegionKind, val left: Double, val top: Double, val width: Double, val height: Double, val marginLeft: Double, val marginTop: Double, val marginRight: Double, val marginBottom: Double, val isActive: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): DuoReservedRegion {
            val kind = DuoRegionKind.fromNative(buf.long)
            val left = buf.double
            val top = buf.double
            val width = buf.double
            val height = buf.double
            val marginLeft = buf.double
            val marginTop = buf.double
            val marginRight = buf.double
            val marginBottom = buf.double
            val isActive = (buf.get().toInt() != 0)
            return DuoReservedRegion(kind, left, top, width, height, marginLeft, marginTop, marginRight, marginBottom, isActive)
        }
        @JvmStatic fun decode(bytes: ByteArray): DuoReservedRegion {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): DuoReservedRegion = DuoReservedRegion(
                kind = DuoRegionKind.fromNative(map["kind"] as Long),
                left = (map["left"] as Number).toDouble(),
                top = (map["top"] as Number).toDouble(),
                width = (map["width"] as Number).toDouble(),
                height = (map["height"] as Number).toDouble(),
                marginLeft = (map["marginLeft"] as Number).toDouble(),
                marginTop = (map["marginTop"] as Number).toDouble(),
                marginRight = (map["marginRight"] as Number).toDouble(),
                marginBottom = (map["marginBottom"] as Number).toDouble(),
                isActive = map["isActive"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(73) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(kind.nativeValue)
        writeDouble(left)
        writeDouble(top)
        writeDouble(width)
        writeDouble(height)
        writeDouble(marginLeft)
        writeDouble(marginTop)
        writeDouble(marginRight)
        writeDouble(marginBottom)
        writeBool(isActive)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, DuoReservedRegion> support ---
    fun toJson(): Map<String, Any?> = mapOf("kind" to kind.nativeValue, "left" to left, "top" to top, "width" to width, "height" to height, "marginLeft" to marginLeft, "marginTop" to marginTop, "marginRight" to marginRight, "marginBottom" to marginBottom, "isActive" to isActive)

}

@androidx.annotation.Keep
data class DuoState(val isSupported: Boolean, val hingeStatus: DuoHingeStatus, val verticalBarEdge: DuoVerticalBarEdge, val hingeAngle: Double?, val regions: List<DuoReservedRegion>, val cornerInsets: DuoInsets) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): DuoState {
            val isSupported = (buf.get().toInt() != 0)
            val hingeStatus = DuoHingeStatus.fromNative(buf.long)
            val verticalBarEdge = DuoVerticalBarEdge.fromNative(buf.long)
            val hingeAngle = (if (buf.get().toInt() == 0) null else buf.double)
            val regions = (0 until buf.int).map { DuoReservedRegion.decodeFrom(buf) }
            val cornerInsets = DuoInsets.decodeFrom(buf)
            return DuoState(isSupported, hingeStatus, verticalBarEdge, hingeAngle, regions, cornerInsets)
        }
        @JvmStatic fun decode(bytes: ByteArray): DuoState {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): DuoState = DuoState(
                isSupported = map["isSupported"] as Boolean,
                hingeStatus = DuoHingeStatus.fromNative(map["hingeStatus"] as Long),
                verticalBarEdge = DuoVerticalBarEdge.fromNative(map["verticalBarEdge"] as Long),
                hingeAngle = (map["hingeAngle"] as Number?)?.toDouble(),
                regions = @Suppress("UNCHECKED_CAST") (map["regions"] as List<DuoReservedRegion>),
                cornerInsets = DuoInsets.fromJson(@Suppress("UNCHECKED_CAST") (map["cornerInsets"] as Map<String, Any?>))
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(94) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(isSupported)
        writeInt(hingeStatus.nativeValue)
        writeInt(verticalBarEdge.nativeValue)
        out.write(if (hingeAngle == null) 0 else 1)
        hingeAngle?.let { writeDouble(it) }
        writeInt32(regions.size)
        regions.forEach { e -> e.writeFieldsTo(out, buf) }
        cornerInsets.writeFieldsTo(out, buf)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, DuoState> support ---
    fun toJson(): Map<String, Any?> = mapOf("isSupported" to isSupported, "hingeStatus" to hingeStatus.nativeValue, "verticalBarEdge" to verticalBarEdge.nativeValue, "hingeAngle" to hingeAngle, "regions" to regions, "cornerInsets" to cornerInsets.toJson())

}

@androidx.annotation.Keep
data class DuoBarPress(val viewId: Long, val index: Long, val menuIndex: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): DuoBarPress {
            val viewId = buf.long
            val index = buf.long
            val menuIndex = buf.long
            return DuoBarPress(viewId, index, menuIndex)
        }
        @JvmStatic fun decode(bytes: ByteArray): DuoBarPress {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): DuoBarPress = DuoBarPress(
                viewId = (map["viewId"] as Number).toLong(),
                index = (map["index"] as Number).toLong(),
                menuIndex = (map["menuIndex"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(24) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeInt(viewId.toLong())
        writeInt(index.toLong())
        writeInt(menuIndex.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, DuoBarPress> support ---
    fun toJson(): Map<String, Any?> = mapOf("viewId" to viewId, "index" to index, "menuIndex" to menuIndex)

}

@androidx.annotation.Keep
data class NitroNullableInt(val hasValue: Boolean, val value: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableInt {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.long
            return NitroNullableInt(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableInt {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableInt = NitroNullableInt(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Long?) : this(v != null, v ?: 0L)
    val nullable: Long? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeInt(value.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableInt> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroNullableDouble(val hasValue: Boolean, val value: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableDouble {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.double
            return NitroNullableDouble(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableDouble {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableDouble = NitroNullableDouble(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toDouble()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Double?) : this(v != null, v ?: 0.0)
    val nullable: Double? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeDouble(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableDouble> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroNullableBool(val hasValue: Boolean, val value: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroNullableBool {
            val hasValue = (buf.get().toInt() != 0)
            val value = (buf.get().toInt() != 0)
            return NitroNullableBool(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroNullableBool {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            buf.position(4) // skip 4-byte length prefix
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroNullableBool = NitroNullableBool(
                hasValue = map["hasValue"] as Boolean,
                value = map["value"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(2) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Boolean?) : this(v != null, v ?: false)
    val nullable: Boolean? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeBool(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        val payload = out.toByteArray()
        val lenBuf = java.nio.ByteBuffer.allocate(4).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        lenBuf.putInt(payload.size)
        return lenBuf.array() + payload
    }

    // --- toJson/fromJson for Map<String, NitroNullableBool> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptInt64(val hasValue: Boolean, val value: Long) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptInt64 {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.long
            return NitroOptInt64(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptInt64 {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptInt64 = NitroOptInt64(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toLong()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Long?) : this(v != null, v ?: 0L)
    val nullable: Long? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeInt(value.toLong())
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptInt64> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptFloat64(val hasValue: Boolean, val value: Double) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptFloat64 {
            val hasValue = (buf.get().toInt() != 0)
            val value = buf.double
            return NitroOptFloat64(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptFloat64 {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptFloat64 = NitroOptFloat64(
                hasValue = map["hasValue"] as Boolean,
                value = (map["value"] as Number).toDouble()
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(9) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Double?) : this(v != null, v ?: 0.0)
    val nullable: Double? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeDouble(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptFloat64> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

@androidx.annotation.Keep
data class NitroOptBool(val hasValue: Boolean, val value: Boolean) {
    companion object {
        @JvmStatic fun decodeFrom(buf: java.nio.ByteBuffer): NitroOptBool {
            val hasValue = (buf.get().toInt() != 0)
            val value = (buf.get().toInt() != 0)
            return NitroOptBool(hasValue, value)
        }
        @JvmStatic fun decode(bytes: ByteArray): NitroOptBool {
            val buf = java.nio.ByteBuffer.wrap(bytes).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            return decodeFrom(buf)
        }
        @Suppress("UNCHECKED_CAST")
        @JvmStatic fun fromJson(map: Map<String, Any?>): NitroOptBool = NitroOptBool(
                hasValue = map["hasValue"] as Boolean,
                value = map["value"] as Boolean
        )
        // Thread-local encode buffers — avoids allocation per bridge call.
        val _tlsOut = ThreadLocal.withInitial { java.io.ByteArrayOutputStream(2) }
        val _tlsBuf = ThreadLocal.withInitial { java.nio.ByteBuffer.allocate(8).order(java.nio.ByteOrder.LITTLE_ENDIAN) }
    }
    constructor(v: Boolean?) : this(v != null, v ?: false)
    val nullable: Boolean? get() = if (hasValue) value else null

    fun writeFieldsTo(out: java.io.ByteArrayOutputStream, buf: java.nio.ByteBuffer) {
        fun writeInt(v: Long) { buf.clear(); buf.putLong(v); out.write(buf.array()) }
        fun writeInt32(v: Int) { buf.clear(); buf.putInt(v); out.write(buf.array(), 0, 4) }
        fun writeDouble(v: Double) { buf.clear(); buf.putDouble(v); out.write(buf.array()) }
        fun writeBool(v: Boolean) { out.write(if (v) 1 else 0) }
        fun writeString(v: String) { val b = v.toByteArray(Charsets.UTF_8); writeInt32(b.size); out.write(b) }
        writeBool(hasValue)
        writeBool(value)
    }

    fun encode(): ByteArray {
        val out = _tlsOut.get()!!.also { it.reset() }
        val buf = _tlsBuf.get()!!
        writeFieldsTo(out, buf)
        return out.toByteArray()  // NO 4-byte length prefix
    }

    // --- toJson/fromJson for Map<String, NitroOptBool> support ---
    fun toJson(): Map<String, Any?> = mapOf("hasValue" to hasValue, "value" to value)

}

/**
 * Contract for the [NitroFoldDuo] module.
 * Implement this in your Kotlin source code.
 * Nitro may call this implementation from any JNI thread.
 * Keep mutable state thread-safe or marshal work onto your own dispatcher.
 */
interface HybridNitroFoldDuoSpec {
    val applicationContext: Context get() = NitroFoldDuoJniBridge.applicationContext
    val activity: Activity? get() = NitroFoldDuoJniBridge.activity

    // Optional lifecycle hooks — override only what you need.
    fun onAttached() {}
    fun onDetached() {}
    fun onActivityAttached(activity: Activity) {}
    fun onActivityDetached() {}

    // source: nitro_fold_duo.native.dart:154
    fun currentState(): DuoState
    // source: nitro_fold_duo.native.dart:172
    fun updateGlassCapsule(viewId: Long, symbols: List<String>, titles: List<String>, selectedIndex: Long, tint: Long, symbolPointSize: Double, isDark: Boolean): Unit
    // source: nitro_fold_duo.native.dart:189
    fun updateGlassSurface(viewId: Long, cornerRadius: Double, tint: Long, isDark: Boolean): Unit
    // source: nitro_fold_duo.native.dart:202
    fun setGlassCapsuleMenu(viewId: Long, buttonIndex: Long, titles: List<String>, symbols: List<String>): Unit
    val stateChanges: Flow<DuoState>
    val glassCapsulePresses: Flow<DuoBarPress>
}

@Keep
object NitroFoldDuoJniBridge {
    private val _implementations = java.util.concurrent.ConcurrentHashMap<Long, HybridNitroFoldDuoSpec>()
    private val _idCounter = java.util.concurrent.atomic.AtomicLong(0)
    private var _factory: (() -> HybridNitroFoldDuoSpec)? = null

    lateinit var applicationContext: Context
        private set

    var activity: Activity? = null
        private set

    private val _zeroCopyPool = java.util.concurrent.ConcurrentHashMap<Int, java.util.concurrent.ArrayBlockingQueue<java.nio.ByteBuffer>>()
    private const val _kZeroCopyPoolPerSize = 8

    /// Borrows a direct buffer of exactly [size] bytes, reusing a released one
    /// when available. Use this instead of allocateDirect() for @zeroCopy
    /// returns. Bounded and non-blocking — it never makes a caller wait.
    @JvmStatic fun acquireZeroCopyBuffer(size: Int): java.nio.ByteBuffer {
        val reused = _zeroCopyPool[size]?.poll()
        if (reused != null) { reused.clear(); return reused }
        return java.nio.ByteBuffer.allocateDirect(size).order(java.nio.ByteOrder.nativeOrder())
    }

    /// Called from the native release path once Dart has released the view.
    /// offer() drops the buffer when the pool is full, so memory stays bounded.
    @JvmStatic fun releaseZeroCopyBuffer(buf: java.nio.ByteBuffer) {
        if (!buf.isDirect) return
        _zeroCopyPool.computeIfAbsent(buf.capacity()) {
            java.util.concurrent.ArrayBlockingQueue(_kZeroCopyPoolPerSize)
        }.offer(buf)
    }

    @JvmStatic external fun initialize(bridgeClass: Class<*>)

    fun registerFactory(factory: () -> HybridNitroFoldDuoSpec, context: Context) {
        applicationContext = context
        _factory = factory
        if (_implementations.isEmpty()) { initialize(this::class.java) }
    }

    @JvmStatic
    fun create_instance_call(key: String): Long {
        val factory = _factory ?: throw IllegalStateException(
            "NitroFoldDuo: no factory registered. Call registerFactory() in onAttachedToEngine().")
        val id = _idCounter.getAndIncrement()
        val impl = factory()
        _implementations[id] = impl
        impl.onAttached()
        return id
    }

    @JvmStatic
    fun destroy_instance_call(instanceId: Long) {
        _implementations.remove(instanceId)?.onDetached()
    }

    fun onActivityAttached(newActivity: Activity) {
        activity = newActivity
        _implementations.values.forEach { it.onActivityAttached(newActivity) }
    }

    fun onActivityDetached() {
        activity = null
        _implementations.values.forEach { it.onActivityDetached() }
    }

    // source: nitro_fold_duo.native.dart:154
    @JvmStatic fun currentState_call(instanceId: Long): ByteArray {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroFoldDuo instance $instanceId not registered")
        val result = impl.currentState()
        return result.encode()
    }
    // source: nitro_fold_duo.native.dart:172
    @JvmStatic fun updateGlassCapsule_call(instanceId: Long, viewId: Long, symbols: ByteArray, titles: ByteArray, selectedIndex: Long, tint: Long, symbolPointSize: Double, isDark: Boolean): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroFoldDuo instance $instanceId not registered")
        val symbolsBuf = java.nio.ByteBuffer.wrap(symbols).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        symbolsBuf.getInt() // skip outer length
        val symbolsCount = symbolsBuf.getInt()
        val symbolsOffsets = LongArray(symbolsCount) { symbolsBuf.getLong() }
        val symbolsDecoded = ArrayList<String>()
        for (symbolsOffset in symbolsOffsets) {
            val itemBuf = java.nio.ByteBuffer.wrap(symbols, 4 + symbolsOffset.toInt(), symbols.size - 4 - symbolsOffset.toInt()).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            val len = itemBuf.getInt()
            val strBytes = ByteArray(len)
            itemBuf.get(strBytes)
            symbolsDecoded.add(strBytes.toString(Charsets.UTF_8))
        }
        val titlesBuf = java.nio.ByteBuffer.wrap(titles).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        titlesBuf.getInt() // skip outer length
        val titlesCount = titlesBuf.getInt()
        val titlesOffsets = LongArray(titlesCount) { titlesBuf.getLong() }
        val titlesDecoded = ArrayList<String>()
        for (titlesOffset in titlesOffsets) {
            val itemBuf = java.nio.ByteBuffer.wrap(titles, 4 + titlesOffset.toInt(), titles.size - 4 - titlesOffset.toInt()).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            val len = itemBuf.getInt()
            val strBytes = ByteArray(len)
            itemBuf.get(strBytes)
            titlesDecoded.add(strBytes.toString(Charsets.UTF_8))
        }
        impl.updateGlassCapsule(viewId, symbolsDecoded, titlesDecoded, selectedIndex, tint, symbolPointSize, isDark)
    }
    // source: nitro_fold_duo.native.dart:189
    @JvmStatic fun updateGlassSurface_call(instanceId: Long, viewId: Long, cornerRadius: Double, tint: Long, isDark: Boolean): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroFoldDuo instance $instanceId not registered")
        impl.updateGlassSurface(viewId, cornerRadius, tint, isDark)
    }
    // source: nitro_fold_duo.native.dart:202
    @JvmStatic fun setGlassCapsuleMenu_call(instanceId: Long, viewId: Long, buttonIndex: Long, titles: ByteArray, symbols: ByteArray): Unit {
        val impl = _implementations[instanceId] ?: throw IllegalStateException("NitroFoldDuo instance $instanceId not registered")
        val titlesBuf = java.nio.ByteBuffer.wrap(titles).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        titlesBuf.getInt() // skip outer length
        val titlesCount = titlesBuf.getInt()
        val titlesOffsets = LongArray(titlesCount) { titlesBuf.getLong() }
        val titlesDecoded = ArrayList<String>()
        for (titlesOffset in titlesOffsets) {
            val itemBuf = java.nio.ByteBuffer.wrap(titles, 4 + titlesOffset.toInt(), titles.size - 4 - titlesOffset.toInt()).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            val len = itemBuf.getInt()
            val strBytes = ByteArray(len)
            itemBuf.get(strBytes)
            titlesDecoded.add(strBytes.toString(Charsets.UTF_8))
        }
        val symbolsBuf = java.nio.ByteBuffer.wrap(symbols).order(java.nio.ByteOrder.LITTLE_ENDIAN)
        symbolsBuf.getInt() // skip outer length
        val symbolsCount = symbolsBuf.getInt()
        val symbolsOffsets = LongArray(symbolsCount) { symbolsBuf.getLong() }
        val symbolsDecoded = ArrayList<String>()
        for (symbolsOffset in symbolsOffsets) {
            val itemBuf = java.nio.ByteBuffer.wrap(symbols, 4 + symbolsOffset.toInt(), symbols.size - 4 - symbolsOffset.toInt()).order(java.nio.ByteOrder.LITTLE_ENDIAN)
            val len = itemBuf.getInt()
            val strBytes = ByteArray(len)
            itemBuf.get(strBytes)
            symbolsDecoded.add(strBytes.toString(Charsets.UTF_8))
        }
        impl.setGlassCapsuleMenu(viewId, buttonIndex, titlesDecoded, symbolsDecoded)
    }
    private val _streamJobs = java.util.concurrent.ConcurrentHashMap<Pair<String, Long>, kotlinx.coroutines.Job>()

    @JvmStatic external fun emit_stateChanges(dartPort: Long, item: ByteArray): Boolean

    @JvmStatic fun nitro_fold_duo_register_state_changes_stream_call(instanceId: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: return
        _streamJobs[Pair("stateChanges", dartPort)] = CoroutineScope(Dispatchers.Default).launch(start = CoroutineStart.UNDISPATCHED) {
            impl.stateChanges.collect { item -> 
                if (!emit_stateChanges(dartPort, item.encode())) {
                    _streamJobs.remove(Pair("stateChanges", dartPort))?.cancel()
                    return@collect
                }
            }
        }
    }
    @JvmStatic fun nitro_fold_duo_release_state_changes_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("stateChanges", dartPort))?.cancel()
    }
    @JvmStatic external fun emit_glassCapsulePresses(dartPort: Long, item: ByteArray): Boolean

    @JvmStatic fun nitro_fold_duo_register_glass_capsule_presses_stream_call(instanceId: Long, dartPort: Long) {
        val impl = _implementations[instanceId] ?: return
        _streamJobs[Pair("glassCapsulePresses", dartPort)] = CoroutineScope(Dispatchers.Default).launch(start = CoroutineStart.UNDISPATCHED) {
            impl.glassCapsulePresses.collect { item -> 
                if (!emit_glassCapsulePresses(dartPort, item.encode())) {
                    _streamJobs.remove(Pair("glassCapsulePresses", dartPort))?.cancel()
                    return@collect
                }
            }
        }
    }
    @JvmStatic fun nitro_fold_duo_release_glass_capsule_presses_stream_call(dartPort: Long) {
        _streamJobs.remove(Pair("glassCapsulePresses", dartPort))?.cancel()
    }
}
